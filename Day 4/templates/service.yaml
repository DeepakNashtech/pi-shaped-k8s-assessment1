apiVersion: v1
kind: Service
metadata:
  name: {{ include "hello-express.fullname" . }}
  labels:
    {{- include "hello-express.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hello-express.selectorLabels" . | nindent 4 }}
